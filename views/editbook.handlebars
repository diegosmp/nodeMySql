<h1>Edite seu livro</h1>
<form action="/books/updatebook" method="post">
    <input type="hidden" name="id" value="{{book.id}}">
    <div class="form-control">
        <input type="text" name="title" placeholder="Digite o título" value="{{book.title}}">
        <input type="number" name="qtdpages" placeholder="Número de páginas" value="{{book.pageqty}}">
    </div>
    <button>Editar</button>
</form>
